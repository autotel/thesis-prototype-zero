/*encoder is conected to a muxb addr*/
/*may be able to reduce dynamic memory by using https://www.arduino.cc/en/Reference/PROGMEM progmem */

/*perhaps I have to work with events that have time and info 
instead of using predefined memory slots. something more like
a hashmap rather than arrays. perhaps a standard midi*/
/*

uint [frame,type, vala, valb] 
0xFF

 */
